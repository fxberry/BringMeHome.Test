class Api {

    //public getUserByName = (userName:string) => {
    //    return browser.executeAsyncScript((el, userName, callback) => {
    //        var api = angular.element(el).injector().get($injections.tests.api);
    //        api.getUserByName(userName).then(data => {
    //            callback(data.data);
    //        }, (error, status) => {
    //            callback(error);
    //        });
    //    }, browser.rootEl, userName);
    //};
}

export = Api